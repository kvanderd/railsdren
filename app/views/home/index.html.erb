<h1>Welcome to Molly Math</h1>
	<div class="span5">
		<h2>Select Your Grade Level</h2>
	</div>
	<div class="wrapper">
		<% @grades.each do |grade| %>
		  <%= link_to grade.name, grade_decks_path(grade.id) %>
		<% end %>
	  <div class="molly"></div>
	</div>
</div>

